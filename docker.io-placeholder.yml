# Docker.io Publishing Placeholder
# This file serves as a placeholder for future docker.io publishing configuration
# When ready to publish to docker.io, uncomment and configure the following:

# docker-publish:
#   runs-on: ubuntu-latest
#   needs: docker-production
#   if: github.event_name == 'workflow_dispatch'
#   steps:
#     - name: Login to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKERHUB_USERNAME }}
#         password: ${{ secrets.DOCKERHUB_TOKEN }}
#
#     - name: Push to Docker Hub
#       run: |
#         # Tag and push the production image to docker.io
#         docker pull ghcr.io/${{ github.repository_owner }}/platform-registration-service:${{ needs.release.outputs.version }}
#         docker tag ghcr.io/${{ github.repository_owner }}/platform-registration-service:${{ needs.release.outputs.version }} \
#           ${{ github.repository_owner }}/platform-registration-service:${{ needs.release.outputs.version }}
#         docker tag ghcr.io/${{ github.repository_owner }}/platform-registration-service:${{ needs.release.outputs.version }} \
#           ${{ github.repository_owner }}/platform-registration-service:latest
#         docker push ${{ github.repository_owner }}/platform-registration-service:${{ needs.release.outputs.version }}
#         docker push ${{ github.repository_owner }}/platform-registration-service:latest

# Required secrets for docker.io publishing:
# - DOCKERHUB_USERNAME: Your Docker Hub username
# - DOCKERHUB_TOKEN: Your Docker Hub access token

# To enable docker.io publishing:
# 1. Add the above job to the workflow
# 2. Set up the required secrets in GitHub repository settings
# 3. Uncomment the job and configure as needed
